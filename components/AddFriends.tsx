import React from "react";
import { ScrollView, Text } from "react-native";

function AddFriends() {
    return (
        <ScrollView>
            <Text>hi my name is gunwoo</Text>
        </ScrollView>
    );
}
